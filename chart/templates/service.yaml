apiVersion: v1
kind: Service
metadata:
  name: {{ include "sqlguard.fullname" . }}
  labels: {{- include "sqlguard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "sqlguard.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
